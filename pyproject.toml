[tool.uv.workspace]
# Python members only: apps/web is a Node project and must stay out of the uv workspace.
members = ["packages/*", "apps/local-server"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
